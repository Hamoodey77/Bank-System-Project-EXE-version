.
🇬🇧 English Summary: C24 Bank System
📝 Program Description
 * This program is a banking management system that handles data for clients, employees, and admins.
 * It uses TXT files for data management.
 * It allows clearing data for each type individually or all at once.
 * It provides interactive menus to manage operations.
 * The program runs through the console only.
⚙️ How to Run
 * Required Files: The program depends on .txt data files.
 * File Location: They MUST be in the same directory as the EXE. The required files are Clients.txt, Employees.txt, Admins.txt, and any other files the system uses.
 * Start: Double-click the BankSystem.exe file.
 * Note: If a file error appears, it means one of the TXT files is missing from the EXE folder.
🔑 Login Requirements
 * The program asks for a Username and Password.
 * Both must be between 8 and 20 characters.
👤 Admin Account Management
 * Admin accounts cannot be created from inside the program.
 * They must be added manually to the Admins.txt file.
How to Add a New Admin:
 * Open Admins.txt.
 * Add a line in this format: AdminID,AdminName,AdminPassword,AdminSalary
 * Open AdminLastID.txt and change the value (e.g., from 3000 to 3001).




🇸🇦 الملخص بالعربية: نظام C24 Bank
📝 وصف البرنامج
 * هذا البرنامج هو نظام لإدارة البنوك يتعامل مع بيانات العملاء والموظفين والمدراء.
 * يستخدم ملفات TXT لإدارة البيانات.
 * يتيح مسح البيانات لكل نوع على حدة أو مسحها كلها دفعة واحدة.
 * يوفر قوائم تفاعلية لإدارة العمليات.
 * يعمل البرنامج من خلال سطر الأوامر (الكونسول) فقط.
⚙️ كيفية التشغيل
 * الملفات المطلوبة: يعتمد البرنامج على ملفات بيانات بامتداد .txt.
 * مكان الملفات: يجب أن تكون هذه الملفات حتمًا في نفس المجلد الذي يحتوي على الملف التنفيذي (EXE). الملفات المطلوبة هي Clients.txt و Employees.txt و Admins.txt وأي ملفات أخرى يستخدمها النظام.
 * بدء التشغيل: يتم تشغيله بالنقر المزدوج على ملف BankSystem.exe.
 * تنبيه: إذا ظهر خطأ في الملف، فهذا يعني أن أحد ملفات TXT مفقود من مجلد الملف التنفيذي.
🔑 متطلبات تسجيل الدخول
 * يطلب البرنامج اسم المستخدم وكلمة المرور.
 * يجب أن يتراوح طول كل منهما بين 8 و 20 حرفًا.
👤 إدارة حسابات المدراء (Admin Account)
 * لا يمكن إنشاء حسابات المدراء من داخل البرنامج.
 * يجب إضافتهم يدويًا إلى ملف Admins.txt.
طريقة إضافة مدير جديد:
 * افتح ملف Admins.txt.
 * أضف سطرًا بهذا التنسيق: AdminID,AdminName,AdminPassword, AdminSalary
 * افتح ملف AdminLastID.txt وقم بتغيير القيمة (على سبيل المثال، من 3000 إلى 3001).
